import { Shadows } from '@mui/material/styles';

const defaultShadow = '0 1px 3px rgba(0,0,0,0.3)';

export const shadows: Shadows = [
  'none',
  defaultShadow, // elevation 1
  '0 4px 12px rgba(0,0,0,0.4)', // elevation 2
  defaultShadow,
  '0 8px 24px rgba(0,0,0,0.5)', // elevation 4
  defaultShadow,
  defaultShadow,
  defaultShadow,
  '0 16px 40px rgba(15,23,42,0.7)', // elevation 8
  defaultShadow,
  defaultShadow,
  defaultShadow,
  defaultShadow,
  defaultShadow,
  defaultShadow,
  defaultShadow,
  defaultShadow,
  defaultShadow,
  defaultShadow,
  defaultShadow,
  defaultShadow,
  defaultShadow,
  defaultShadow,
  defaultShadow,
  defaultShadow,
] as Shadows;
